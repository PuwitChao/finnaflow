/** Application version — bump in sync with VERSION file and CHANGELOG.md. */
export const APP_VERSION = '2.2.0-Alpha';

/** Average weeks in a calendar month (52 / 12). Used for weekly→monthly normalization. */
export const WEEKS_PER_MONTH = 52 / 12;
